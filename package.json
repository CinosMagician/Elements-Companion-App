{
  "name": "elementsthegame",
  "version": "1.0.0",
  "description": "Requests DJ MERN styles",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "installation": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "render-build":"npm run installation && npm run build",
    "seed": "cd server && node ./utils/seedCards.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
