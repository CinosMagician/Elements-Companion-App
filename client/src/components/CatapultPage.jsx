import React, { useEffect, useState } from 'react';
import { useQuery } from '@apollo/client';
import { GET_CARDS } from '../utils/queries';
import CardCanvas from '../components/CardCanvas';

const CatapultPage = () => {    
    return (
        <div>
            Catapult
        </div>
    );  
}

export default CatapultPage;